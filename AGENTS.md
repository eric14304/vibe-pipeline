# AGENTS.md

本檔給 codex 等不認 claude `CLAUDE.md` + `.claude/skills/` 系統的 AI 看。

**動 code 前必讀**:同目錄 [`CLAUDE.md`](CLAUDE.md)。

CLAUDE.md 內含 repo 結構、Phase 進度、架構決策、雷區、設計信條、dir → SKILL 路由表、path → rule 路由表。本檔不複製內容,避免兩份 drift。

claude CLI 自動載 CLAUDE.md;codex 不會,所以你看到本檔就主動讀。
